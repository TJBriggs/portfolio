/**
 * @example
 * <RoundedBox
 *    @param {}  - ()
 * />
 */
const RoundedBox = ({ children }) => {
	return (
		<div className='rounded-box'>
			{children}
		</div>
	);
};

export default RoundedBox;
